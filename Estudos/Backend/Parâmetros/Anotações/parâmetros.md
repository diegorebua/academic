# Parâmetros em requisições

> TODO: adicionar anotações sobre tipos de parâmetros em APIs.

Sugestão de tópicos:
- Query params (`/users?age=20`)
- Route params (`/users/:id`)
- Body (`POST /users` com JSON)
- Headers (ex: `Authorization`)
- Boas práticas de validação e tipagem

